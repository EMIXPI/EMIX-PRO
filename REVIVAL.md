# REVIVAL.md — احیای EMIX-PRO بر اساس پروتکل پایه‌ی EMIX (v12.0.0-core)

> تاریخ: 2026-09-04 · گزارش کاربر: «پنل باز می‌شود ولی کانفیگ‌ها وصل نمی‌شوند —
> EMIX اصلی پینگ می‌دهد، EMIX-PRO نه. می‌خواهیم EMIX-PRO را بر اساس پروتکل پایه‌ی
> EMIX احیا کنیم و بعد به قدرت vpn-ui برسد — بدون اینکه هسته خراب شود.»

---

## 1. تشخیص (چرا کانفیگ‌ها می‌مردند؟)

سه لایه‌ی مشکل روی هم انباشته بودند:

### 🔴 لایه‌ی ۱ — باگ هویت: چرخش UUID با هر ری‌دیپلوی (علت ریشه‌ای حادثه)

در `_get_or_create_secret()` ساختار try/except شکسته بود: `DATA_DIR.mkdir`
داخل همان try بود؛ روی Railway **بدون Volume** (`/data` قابل نوشتن نیست)
خطای Permission کل زنجیره را می‌پراند و **بدون رسیدن به fallback پایدارِ
`RAILWAY_SERVICE_ID`**، secret تصادفی برمی‌گرداند:

```
SECRET_KEY ──✗──▶ فایل Volume ──✗(mkdir PermissionError → پرش به except!)──▶ رندوم ❌
                                     (RAILWAY_SERVICE_ID هرگز بررسی نمی‌شد)
```

نتیجه: هر git-push → ری‌دیپلوی → secret جدید → UUID همه‌ی کانفیگ‌های
پیش‌فرض عوض → همه‌ی کانفیگ‌های تحویل‌شده به کلاینت‌ها با `1008 not authorized`
رد می‌شدند. **دقیقاً «پنل باز می‌شود ولی کانفیگ‌ها وصل نمی‌شوند».**

همچنین برچسب `IDENTITY_STABLE` همیشه `true` گزارش می‌شد (مقایسه با مقداری که
دیگر تولید نمی‌شد) — مانیتورینگ کور شده بود.

**فیکس (main.py):** خطای دیسک فقط warning است و زنجیره کامل طی می‌شود:
`SECRET_KEY → فایل Volume → RAILWAY_SERVICE_ID / EMIX_IDENTITY_SEED → رندوم
(با CRITICAL صادقانه)`. برچسب پایداری هم حالا درست است.
رگرسیون‌تست: `tests/unit/test_identity_stability.py §2.b` (دو تست جدید؛ روی
کد قدیمی fail می‌شوند — تست‌شده).

### 🟠 لایه‌ی ۲ — بوتِ شلوغ: سلامت هسته گروگان ۲۷ موتور اختیاری

v11 همه‌چیز را در بوت import می‌کرد: ۴۶.۵هزار خط، ۳۱۶ روت. نسخه‌ی سالمِ EMIX
همان ۱۴هزار خط با بوت سبک است. هیچ قراردادی وجود نداشت که «هسته» (پینگ/رله/ساب)
از «موتورها» جدا باشد — هر ماژول می‌توانست بوت را سنگین یا شکننده کند.

**فیکس (`boot_profile.py` جدید + gate در main.py):**

| پروفایل | رفتار | روت‌ها | مصرف |
|---|---|---|---|
| `EMIX_PROFILE=core` (**پیش‌فرض**) | فقط هسته‌ی همیشه‌زنده | ۱۴۱ | سبک، هم‌تراز EMIX |
| `EMIX_PROFILE=full` | دقیقاً رفتار v11 | ۳۱۷ | مثل قبل |
| `EMIX_ENABLE=a,b` | core + موتورهای منتخب | — | — |
| `EMIX_DISABLE=a,b` | full منهای منتخب‌ها | — | — |

**هسته‌ی همیشه‌زنده (هرگز gate ندارد):** `/api/ping` (healthcheck)، `/health`،
رله‌ی `WS /ws/{uuid}` (VLESS) و `/trojan-ws` و `/ss-ws` (Shadowsocks)، XHTTP،
MTProto، لینک‌ها/سابسکریپشن `/sub/{uuid}`، داشبورد و لاگین، کامپایلر کانفیگ،
jobهای حیاتی، بکاپ. **۲۷ موتور PRO** (گیمینگ، مولتی‌لوک، موتورهای
راستی‌آزمایی v11.2+، حساب‌ها، SNI/VPN-Pro، IP-Quality، …) حالا **اختیاری‌اند**:
خاموشی‌شان «انتخاب» است نه خطا — و هیچ‌کدام دیگر نمی‌توانند پینگ را گروگان
بگیرند.

**Self-check بوت:** بعد از استارت، وجود هر ۸ مسیر پایه راستی‌آزمایی و در لاگ
ثبت می‌شود: `✅ CORE SURFACE OK — 8 مسیر پایه (پینگ/رله/ساب/داشبورد)…` — جایگزین
«سکوت مرگبار». گزارش زنده: `GET /api/boot-profile` (نیازمند لاگین).

### 🟡 لایه‌ی ۳ — تست‌سایت بی‌سیم (کیفیت نتوانست حادثه را ببیند)

۹۵۲ تست «سبز» بود ولی: تست‌های هویت همیشه با DATA_DIR قابل‌نوشتن اجرا می‌شدند
(مسیر Permission تست نشده بود)، تستِ timeout شبکه وابسته به محیط بود، و
state پابرجای بین ران‌ها totals ترافیکی را آلوده می‌کرد.
**فیکس:** رگرسیون‌تست Permission-denied (که روی کد قدیمی واقعاً fail
می‌شود)، تست timeout قطعی‌شده (mock)، پاک‌سازی state در شروع هر session، و
تست‌های جدید `test_core_revival.py` — از جمله **رله‌ی VLESS end-to-end واقعی**
(هندشیک WS → هدر VLESS → TCP به هدف واقعی → رفت‌وبرگشت داده).

---

## 2. قبل / بعد

| سنجه | v11.6.0 (قبل) | v12.0.0-core (بعد) |
|---|---|---|
| UUID کانفیگ‌های پیش‌فرض بدون Volume | 🔄 با هر ری‌دیپلوی عوض می‌شد | 🔒 پایدار (RAILWAY_SERVICE_ID) |
| روت‌های بوت پیش‌فرض | ۳۱۶ (همه‌چیز) | ۱۴۱ (هسته) — ۳۱۷ با `full` |
| ضمانت پینگ | ضمنی | قرارداد + self-check بوت + `/api/boot-profile` |
| رله‌ی VLESS e2e تست‌شده | ❌ | ✅ (WS → VLESS → TCP → roundtrip) |
| تست‌ها | ۹۵۲ | **۹۶۲/۹۶۲ سبز** (۲× اجرای متوالی) |
| بوت | ~0.56s / 316 روت | ~0.54s / 141 روت (core) |

## 3. چک‌لیست دیپلوی (Railway)

1. **Deploy Latest Commit** بزنید (نه Redeploy) — نسخه باید
   `12.0.0-core` را در `/api/deployment-version` نشان دهد.
2. **یک‌بار کانفیگ‌ها را دوباره import کنید** (ساب URL ها را رفرش کنید).
   UUID ها از این به بعد بین ری‌دیپلوی‌ها ثابت می‌مانند؛ این آخرین جابه‌جایی است.
3. توصیه‌شده برای production (هر کدام کافی است):
   - `SECRET_KEY` را در Variables ست کنید (رازِ قوی و پایدار)، یا
   - یک **Volume** به `/data` وصل کنید (state هم پایدار می‌شود).
4. امکانات PRO را چطور برگردانم؟
   - همه با هم: `EMIX_PROFILE=full`
   - منتخب: `EMIX_ENABLE=gaming_boost,multiloc,account_manager` (در core)
5. سلامت بعد از دیپلوی: `/api/ping` باید `{"ok":true}` بدهد؛ در لاگ
   `✅ CORE SURFACE OK` ببینید؛ با لاگین `/api/boot-profile` گزارش کامل.

## 4. نقشه‌ی راه — «به قدرت vpn-ui، بدون شکستن هسته»

قرارداد جدید دقیقاً برای همین هدف است: هر قابلیت تازه = یک موتور اختیاری در
`boot_profile.ENGINES` + route registration پشت `boot_profile.enabled(...)` —
هسته (پینگ/رله/ساب) هرگز ریسک نمی‌شود:

1. **WireGuard runtime واقعی روی نود VPS** (کنترل‌پلن vpn_pro آماده است؛
   موتور agent نود لازم است) — همان مسیر vpn-ui اما با نودهای خودتان.
2. **Hysteria2/TUIC serving** (رجیستری پروتکل آماده؛ adapter serving شود).
3. **Multi-user + quota backend-enforced** (ادامه‌ی account_manager: نقش‌ها،
   تیم، فاکتور).
4. **Observability** (متیریکس Prometheus خروجی بگیرد — فقط موتور جدید).

> قانون طلایی احیا: «هیچ موتوری اجازه ندارد پینگ بدهد یا نگیرد.» — هسته
> با همان پروتکل پایه‌ی EMIX سبک می‌ماند و سالم می‌ماند.
